# Result - NewHMIProject Label DynamicLink Accepted

## Project

```text
NewHMIProject
```

## Test

A second label was added under:

```text
UI/MainWindow/Panel1
```

The label text was configured using a `DynamicLink` to the Model variable:

```text
/Objects/NewHMIProject/Model/StatusText
```

## Result

```text
Partial success / accepted by FT Optix Studio
```

Observed in FT Optix Studio:

```text
Panel1
├─ Label1
└─ Label2
```

The property panel for `Label2` shows the `Text` property linked to:

```text
StatusText
```

This confirms that the generated UI YAML was accepted and that FT Optix created the DynamicLink object.

## Important observation

The design surface displayed:

```text
####
```

This may be only a design-time placeholder for a dynamic value, or it may mean the `LocalizedText` label property is not resolving the linked model `String` value correctly in the designer.

## Next checks

1. Run the project in Emulator and confirm whether `Label2` shows the actual value.
2. Save and close the project, then inspect the normalized `Nodes/UI/UI.yaml` generated by FT Optix.
3. If runtime still shows `####`, test one of these alternatives:

```text
Option A: add DynamicLink Mode = 2
Option B: change the model variable to LocalizedText
Option C: use a converter from String to LocalizedText if required
```

## Meaning

This is the first confirmed step where generated YAML successfully created a UI object with a DynamicLink to a Model variable.
